# Day 2 tutorial: `git`

## Exercise 1 - setting up `git`

### Step 1: install `git` on your system

- [ ] Check that `git` is installed by running `git --version` in a terminal (e.g. `PowerShell`, `Command Prompt` or `git bash` on Windows). You should see something like `git version x.y.z`


## Exercise 2 - 
* Fork the repository
* 